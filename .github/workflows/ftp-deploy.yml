name: 🚀 FRESH START DEPLOY

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 🛠️ Build Frontend
        run: |
          npm install
          npm run build

      - name: 🌐 Upload Site (Frontend)
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./dist/
          server-dir: ./public_html/
          # Добавляем таймаут и отключаем проверку состояния для стабильности
          protocol: ftp
          port: 21

      - name: ⚙️ Upload Server (Backend)
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./
          server-dir: /var/www/lgzt-back/
          # Здесь НЕ чистим всё подряд, чтобы не провоцировать таймауты
          exclude: |
            node_modules/**
            dist/**
            src/**
            .git/**
            .github/**
