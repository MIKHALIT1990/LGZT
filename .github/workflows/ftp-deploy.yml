name: 🚀 Full Deploy (Site + VPS Server)

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4

    - name: 🛠️ Install & Build Frontend
      run: |
        npm install
        npm run build

    # ШАГ 1: Отправляем ФРОНТЕНД (готовый сайт) на хостинг
    - name: 📂 Deploy Frontend to Beget
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: ./dist/
        server-dir: ./public_html/
        # Удали старые файлы, чтобы сайт был чистым
        dangerous-clean-slate: true 

# 2. Отправляем БЭКЕНД на VPS
    - name: 📂 Deploy Backend to VPS
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: ./
        # Пробуем указать путь БЕЗ начального слэша, если FTP сразу кидает в корень
        server-dir: var/www/lgzt-back/ 
        exclude: |
          node_modules/**
          dist/**
          src/**
          .git/**
